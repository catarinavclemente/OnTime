# SASS Compile

```shellscript
cd lib/themes/emn_theme
npx grunt sass     # just compile
npx grunt theme    # compile + autoprefixer + stylelint + string-replace (recommended)
npx grunt          # full pipeline including watch mode
```

